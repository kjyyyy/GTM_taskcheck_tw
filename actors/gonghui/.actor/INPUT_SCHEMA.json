{
  "title": "公會 scraper input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Association directory URLs (override)",
      "type": "array",
      "description": "Override: crawl only these URLs (each uses its host's parser from config/gonghui-sites.json). Empty = crawl every configured site.",
      "editor": "stringList"
    },
    "contactEmail": {
      "title": "Contact email (User-Agent)",
      "type": "string",
      "description": "Sent in the User-Agent so site owners can reach us (politeness).",
      "editor": "textfield"
    }
  },
  "required": []
}
